import type { Metadata } from "next";
import { Playfair_Display, Inter } from "next/font/google";
import "./globals.css";

const playfair = Playfair_Display({
  variable: "--font-playfair",
  subsets: ["latin"],
  display: "swap",
});

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Lumina Foundation | Empowering Communities Worldwide",
  description:
    "Lumina Foundation is dedicated to creating lasting change through education, healthcare, and sustainable development. Join us in building a brighter future.",
  openGraph: {
    title: "Lumina Foundation | Empowering Communities Worldwide",
    description:
      "Creating lasting change through education, healthcare, and sustainable development.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${playfair.variable} ${inter.variable} font-sans antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
